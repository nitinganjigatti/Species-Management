'use client'

import Box from '@mui/material/Box'
import SpeciesListingContainer from 'src/components/species-management/SpeciesListingContainer'

const SpeciesListPage = () => (
  <Box sx={{ minHeight: '100vh' }}>
    <SpeciesListingContainer />
  </Box>
)

export default SpeciesListPage
