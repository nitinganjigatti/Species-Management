'use client'

import Box from '@mui/material/Box'
import VisitorReports from 'src/components/vms/reports/VisitorReports'

const ReportsPage = () => {
  return (
    <Box sx={{ minHeight: '100vh' }}>
      <VisitorReports />
    </Box>
  )
}

export default ReportsPage
